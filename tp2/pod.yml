apiVersion: v1
kind: Pod
metadata:
  name: simple-webapp-color
spec:
  containers:
  - name: color
    image: mmumshad/simple-webapp-color
    ports:
    - containerPort: 8080

    env:
      - name: APP_COLOR
        value: red

